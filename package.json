{
  "name": "enkel-automate",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "node --max-old-space-size=4096 node_modules/.bin/playwright test",
    "start": "npx playwright test slackBotFlow.spec.js && node generateReport.js",
    "test:clean": "rimraf allure-results allure-report && npx playwright test && npx allure generate allure-results --clean -o allure-report"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "allure-playwright": "^3.3.0",
    "dotenv": "^17.0.1",
    "fs": "^0.0.1-security",
    "playwright": "^1.53.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.2",
    "rimraf": "^6.0.1"
  }
}